#include "position.h"

Position::Position(int row, int col) // Construtor da classe Position que inicializa a posição com linha e coluna
{
    this->row = row; // Atribui o valor da linha
    this->col = col; // Atribui o valor da coluna
}